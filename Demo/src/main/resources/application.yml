# datasource
spring:
  datasource:
    url: jdbc:mysql://106.14.32.207:3306/project?serverTimezone=UTC
    username: user
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver


# mybatis
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml # mapper映射文件路径
  type-aliases-package: com.example.demo.domain

  # config-location:  # 指定mybatis的核心配置文件
server:
  port: 8088



helper-dialect: project
reasonable: true
params: pageNum=punt=countSql;reasonable=reasonable;pageSizeZero=pageSizeZero
support-methods-arguments: true
row-bounds-with-count: false
